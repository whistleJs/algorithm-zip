/**
 * 실제 제출 코드
 */
// const readline = require('readline');
// const rl = readline.createInterface({
//   input: process.stdin,
//   output: process.stdout
// }).on('line', line => {
//   console.log([...line].join('\n'));
// });

/**
 * 테스트를 위한 코드
 */
const solution = (line: string) => {
  return [...line].join('\n');
};

export default solution;